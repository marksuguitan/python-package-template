// filepath: /Users/dumarkus/Documents/Repositories/\_templates/python-package-template/{{ cookiecutter.package_slug }}/README.md

# {{ cookiecutter.project_name }}

> {{ cookiecutter.description }}

## Quick Start

1.  Update metadata in `pyproject.toml` if needed (most fields are now templated).
2.  Push to a GitHub repo (e.g., `git@github.com:{{ cookiecutter.github_user }}/{{ cookiecutter.distribution_slug }}.git`).
3.  Tag a release (`git tag v0.1.0 && git push --tags`) to build & publish via GitHub Actions.

_Note: The original instructions about search-replacing are less relevant now that Cookiecutter handles it._

see: https://chatgpt.com/share/6814e042-cdd8-800e-ba3b-f839d02175cf
